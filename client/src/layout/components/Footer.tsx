import { memo } from 'react';

const Footer = () => {
  return (
    <div className='px-6 fixed bottom-0 left-70 bg-[var(--color-bg-py)] dark:bg-[var(--color-dark-bg-py)]'>Footer</div>
  );
};

export default memo(Footer);