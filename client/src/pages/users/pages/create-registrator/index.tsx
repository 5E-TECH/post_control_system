import { memo } from "react";
import CreateUserForm from "../../components/create-user-form";

const CreateRegistrator = () => {
  return <CreateUserForm title="Ro'yxatchi" />;
};

export default memo(CreateRegistrator);
